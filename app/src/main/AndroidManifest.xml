<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.you.cauchy">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!-- 请求访问网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CauchyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.course.CourseListAct"></activity>
        <activity android:name=".activity.user.UserExamListAct" />
        <activity android:name=".activity.user.ExpectAct" />
        <activity android:name=".activity.user.MoneyFlowingAct" />
        <activity android:name=".activity.exam.ExamAct" />
        <activity android:name=".activity.login.RegistryAct" />
        <activity android:name=".activity.comm.ImageDetailsAct" />
        <activity
            android:name=".activity.course.CourseInfoInsAct"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.course.CourseAct"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.home.HomeAct" />
        <activity
            android:name=".activity.login.LoginAct"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.launch.LaunchAct"
            android:screenOrientation="portrait"
            android:theme="@style/WelcomeStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.login.LoginSampleAct"
            android:label="@string/title_activity_login" />
    </application>

</manifest>